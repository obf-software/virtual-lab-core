export enum Ec2InstanceState {
    pending = 'pending',
    running = 'running',
    'shutting-down' = 'shutting-down',
    terminated = 'terminated',
    stopping = 'stopping',
    stopped = 'stopped',
}
